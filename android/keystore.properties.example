# Keystore configuration for release signing
# Copy to keystore.properties and fill in values
# DO NOT commit keystore.properties to git

storeFile=/path/to/soulo-keystore.jks
storePassword=your-store-password
keyAlias=soulo-release
keyPassword=your-key-password
